{
  "name": "music-tools",
  "version": "1.0.0",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "node server.js",
    "kill-server": "PID=$(lsof -ti :3000) && [ -n \"$PID\" ] && kill \"$PID\" || true"
  }
}
